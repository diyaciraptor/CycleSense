{
  "name": "cyclesense",
  "version": "1.0.0",
  "private": true,
  "description": "CycleSense period tracker PWA wrapped with Capacitor for Android.",
  "scripts": {
    "serve": "npx serve .",
    "cap:init": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.1.0",
    "@capacitor/cli": "^7.1.0",
    "@capacitor/core": "^7.1.0"
  }
}
